import { Typography } from "@mui/material";

const TextNotPublic = () => (
	<Typography textAlign="center">
		This text is not public or may have been deleted by the author
	</Typography>
);

export default TextNotPublic;
