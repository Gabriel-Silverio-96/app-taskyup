import { styled } from "@mui/material";

export const CardHeaderContainer = styled("div")(
	() => `
        text-align: right;          
        opacity: 0;
        transition: .3s;
        cursor: pointer;
    `
);
